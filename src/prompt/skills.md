<skills>
Skill summaries are preloaded below to keep context compact. Detailed skill instructions are loaded on demand: use `read` with the `<location>` path when you need the full skill file. The `location` field is included for reference.

<skills-list>
{{skills}}
</skills-list>
</skills>
