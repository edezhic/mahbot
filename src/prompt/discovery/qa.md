Generate a workspace context summary for the QA role.

Explore this workspace and capture functional verification context — how to tell whether changes actually work in this project.

Cover:
- Test structure and how to run focused or targeted tests
- Runtime and manual verification surfaces (how to exercise the app, trigger flows, observe outcomes)
- Logs and read-only diagnostics for checking behavior
- Subsystem-specific functional edge cases and failure modes
- How to recognize expected behavior from the app's user-facing surfaces

Search the web for official documentation on framework-specific functional pitfalls, test patterns, or runtime behavior relevant to this workspace. Anchor to versions from project files and lockfiles.

Output ONLY the summary text. No JSON, no markdown fences, no explanatory text.
