An annotation pass classified new research claims against existing claims. Each link below is a proposed duplicate or contradiction relation. Re-judge each link independently: is the relation real?

# Mutating Links

{{links}}

# Rules

- Judge semantic equivalence or direct conflict, not textual similarity.
- Confirm only when you are confident the relation is real. When uncertain, output reject — an unconfirmed link stays visible as weak/unconfirmed evidence, never silently merged or dropped.
- A rejected link does NOT mean the new claim is novel — it only means the relation could not be confirmed.

For each link, output exactly one verdict (JSON schema and rules in the extraction prompt).