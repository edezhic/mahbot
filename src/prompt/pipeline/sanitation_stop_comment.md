Sanitation could not finish the round: the repository state could not be read or the final commit failed. The round is stopped instead of being retried — once the workspace is resumed the round is replayed from scratch and the commit is attempted again, keeping the uncommitted changes.

{{failure_details}}