⚠️ Engineer failure — workspace paused

{{failure_details}}

---

The Engineer agent failed on this ticket, so the workspace was paused and the ticket was frozen in place (it stays in the development stage with its context preserved). No action is needed right now: once the workspace is unpaused the ticket resumes its development automatically on the same job, with the error above attached as rework feedback. {{workspace_status}}
