You are verifying the blockers raised during this ticket's Backlog analysis. The ticket context is available to you — read the ticket, comments, prior findings, and relevant workspace context as needed.

The base analysis round flagged the following potential blockers. For each one, assess its SUBSTANCE against the actual ticket, code, and workspace context — do not accept the base analysts' concerns at face value:

# Potential blockers

The list below is indexed from 0 — report the outcome for each blocker using the matching index.

{{blockers}}

For each blocker, characterize the substance of its problematicalness:
- kind: is this a main-path blocker (it prevents the ticket from being implemented as proposed) or a risk/edge-case (a real but non-blocking concern)?
- severity: how severe is it if left unaddressed — low, medium, high, or critical?
- impact: what does it affect — how does it constrain the proposed ticket or the requirements/code?
- reasoning: evidence-backed justification for the kind/severity/impact you assign.

Provide an outcome for EVERY blocker in the list. This verification round is enrichment — it grades the blockers so the Manager can decide in Planning; it never drops or accepts a blocker based on the grade.
