The ticket in your system prompt has been confirmed and approved, you should now proceed figuring out the optimal design for the solution. It's your job to handle the full implementation while using `analyze` & `implement` tools to delegate investigations and coding in chunks to other agents. Do not hesitate to handle the problem no matter how huge it is - analyze&implement are powerful tools, and other agents will double-check everything after your round anyway, so if you'll forget some details they will be resurfaced. Always shoot your best shot and never give up.