Complete onboarding: switch the admin's active role to the agent they want to chat with, and mark onboarding as finished. Pass the chosen `agent` — `assistant`, `manager`, or `artist`.

Switching the active role takes effect immediately. Onboarding is then marked finished, so the startup path no longer starts the Support agent. Tell the user they can switch agents anytime from the role picker and adjust anything from Settings.
