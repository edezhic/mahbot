Register a web-search backend so agents can use `web_search`. Pass the `provider` — `exa`(preferred) or `firecrawl` — and its `api_key`. The provider and its matching key are persisted. Once registered, agents that support web search (e.g. the Assistant) can call `web_search` immediately.
