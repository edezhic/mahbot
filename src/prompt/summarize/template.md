Summarize the conversation so far into concise context for the {{role_name}} role.

PRESERVE exactly:
{{role_section}}
- All identifiers (UUIDs, hashes, file paths, URLs, tokens, IPs)

OMIT:
{{extra_omit}}{{omit_section}}
