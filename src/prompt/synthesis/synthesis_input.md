Group the agent issues below into thematic groups. Every group member must reference an
item id from the input list — copy the id exactly, never the text. Item ids are GLOBAL and
FLAT across all agents: each issue has exactly one id, numbered in (agent, issue) order.
You may leave issues ungrouped if they do not fit a theme (put them in the "ungrouped" list).
