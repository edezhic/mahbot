.DS_Store
/target
vendor/**/target/

# cargo-generated lock for the vendored candle-onnx-mahbot fork source
# (it is not its own workspace; its lock is owned by mahbot's root Cargo.lock,
# and `cargo package`/`cargo publish` regenerates it on every run)
vendor/candle-onnx/Cargo.lock
